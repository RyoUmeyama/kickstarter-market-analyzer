# Kickstarter Market Analyzer - クイックスタートガイド

このガイドでは、システムを利用開始するために必要な最小限の設定手順を説明します。

---

## 📋 このガイドで行うこと

1. Google Cloud サービスアカウントの作成（スプレッドシート連携用）
2. OpenAI APIキーの取得（AI分析機能用）
3. GitHub Actionsでのワークフロー実行

**所要時間**: 約20-30分

---

## 1. Google Cloud サービスアカウントの作成

GitHub Actionsからスプレッドシートにアクセスするために、サービスアカウントを作成します。

### ステップ1: Google Cloud Console にアクセス

1. ブラウザで以下のURLを開く:
   ```
   https://console.cloud.google.com/
   ```

2. Googleアカウントでログイン

### ステップ2: プロジェクトを作成

1. 画面上部の「プロジェクトを選択」をクリック

2. 「新しいプロジェクト」をクリック

3. プロジェクト名を入力
   - 例: `kickstarter-analyzer`

4. 「作成」をクリック

5. プロジェクトが作成されたら、そのプロジェクトを選択

### ステップ3: Google Sheets APIを有効化

1. 以下のURLを開く:
   ```
   https://console.cloud.google.com/apis/library/sheets.googleapis.com
   ```

2. 「有効にする」ボタンをクリック

### ステップ4: サービスアカウントを作成

1. 左メニュー → 「IAMと管理」 → 「サービスアカウント」をクリック

2. 「サービスアカウントを作成」をクリック

3. 以下を入力:
   - **サービスアカウント名**: `kickstarter-analyzer`
   - **説明**: `Kickstarter市場分析用`

4. 「作成して続行」をクリック

5. **ロール**: 何も選択せず「続行」をクリック

6. 「完了」をクリック

### ステップ5: サービスアカウントキー（JSON）をダウンロード

1. 作成したサービスアカウントをクリック
   - `kickstarter-analyzer@...iam.gserviceaccount.com` という形式

2. 「キー」タブをクリック

3. 「鍵を追加」 → 「新しい鍵を作成」をクリック

4. **キーのタイプ**: `JSON` を選択

5. 「作成」をクリック

6. **JSONファイルが自動的にダウンロードされます**

⚠️ **重要**: このJSONファイルは安全な場所に保管してください。

### ステップ6: スプレッドシートにサービスアカウントを共有

1. ダウンロードしたJSONファイルをメモ帳などで開く

2. `"client_email":` の後にあるメールアドレスをコピー
   - 例: `kickstarter-analyzer@project-123.iam.gserviceaccount.com`

3. Google Sheetsでスプレッドシートを開く

4. 右上の「共有」ボタンをクリック

5. コピーしたメールアドレスを貼り付け

6. 権限を **「編集者」** に設定

7. 「送信」をクリック

✅ **完了**: サービスアカウントの設定が完了しました

---

## 2. OpenAI APIキーの取得

AIによるレポート生成機能を使用するために、OpenAI APIキーを取得します。

### ステップ1: OpenAIアカウントを作成

1. 以下のURLを開く:
   ```
   https://platform.openai.com/signup
   ```

2. アカウントを作成（既にお持ちの場合はログイン）

### ステップ2: クレジットを追加

1. ログイン後、右上のアイコン → 「Billing」をクリック

2. 「Add payment method」でクレジットカードを登録

3. 「Add credits」でクレジットを追加
   - **推奨**: $10程度で十分です（数ヶ月分）

### ステップ3: APIキーを作成

1. 以下のURLを開く:
   ```
   https://platform.openai.com/api-keys
   ```

2. 「Create new secret key」をクリック

3. 名前を入力
   - 例: `Kickstarter Analyzer`

4. 「Create secret key」をクリック

5. **表示されたAPIキーをコピー**
   - `sk-proj-...` で始まる長い文字列

⚠️ **重要**: APIキーは**一度しか表示されません**。必ずコピーして保存してください。

### 料金の目安

| 項目 | 料金 |
|------|------|
| 1回の処理 | 約1-3円 |
| 月間100件 | 約100-300円 |

✅ **完了**: OpenAI APIキーの取得が完了しました

---

## 3. GitHub Actionsワークフロー実行手順

### ステップ1: GitHubにログイン

1. ブラウザで以下のURLを開く:
   ```
   https://github.com/login
   ```

2. **Username or email address** を入力

3. **Password** を入力

4. 「Sign in」をクリック

5. 2段階認証がある場合は認証コードを入力

### ステップ2: リポジトリにアクセス

1. ログイン後、以下のURLを開く:
   ```
   https://github.com/koki4117/kickstarter-market-analyzer
   ```

### ステップ3: Actionsタブを開く

1. 画面上部のメニューから **「Actions」** をクリック
   ```
   Code | Issues | Pull requests | [Actions] | Projects | Settings
   ```

### ステップ4: ワークフローを選択

1. 左側のメニューから **「管理表から抽出してメール生成」** をクリック

### ステップ5: ワークフローを実行

1. 右上の **「Run workflow」** ボタン（緑色）をクリック

2. ドロップダウンが表示されます:

   | 項目 | 設定 |
   |------|------|
   | Branch | `main`（そのまま） |
   | 抽出するステータス | 処理したいステータスを選択 |

3. ステータスを選択:
   - （空白＝新規）
   - ②無返信2回目　要送信
   - ➂無返信3回目　要送信
   - ④自動返信　要送信
   - ⑤好返信　要レポ送信

4. 緑色の **「Run workflow」** ボタンをクリック

### ステップ6: 実行状況を確認

1. ページをリロード（F5キー）

2. 新しい実行が一覧に表示されます

3. 実行状況のアイコン:

   | アイコン | 状態 |
   |---------|------|
   | 🟡 黄色（回転中） | 実行中 |
   | ✅ 緑色のチェック | 成功 |
   | ❌ 赤色のバツ | 失敗 |

4. 通常 **1-3分** で完了します

### ステップ7: 結果を確認

1. Google Sheetsの **kickstarter** シートを開く

2. 処理された行に結果が入力されています:

   | 列 | 内容 |
   |----|------|
   | F列 | 日本語件名 |
   | G列 | 英語件名 |
   | H列 | 日本語本文 |
   | I列 | 英語本文 |
   | J列 | 日本語本文（HTML） |
   | K列 | 英語本文（HTML） |

【完了】 ワークフローの実行が完了しました
