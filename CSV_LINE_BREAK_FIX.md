# CSV Line Break Fix - Implementation Guide

**Issue Date**: 2025-11-14
**Status**: Fix Implemented - Awaiting Testing

---

## 問題の概要

Google Sheetsからエクスポートしたkickstarter.csvファイルをメールマージで使用する際、`jp_body`および`en_body`列の本文が全て連結された状態になり、改行が失われて読みにくい状態になる。

### 問題の具体例

CSVファイル内の`jp_body`列（11列目）の内容：
```
メーカー名不明 Sales Team
お世話になっております。
先日ご提案に関して、以下の貴社製品の日本市場における販売拡大可能性を調査いたしました。
https://www.kickstarter.com/projects/...
フェーズ1：クラウドファンディング
フェーズ2：アマゾン等のECサイト販売
...（全ての文章が1行に連結）
```

**期待される出力**:
```
メーカー名不明 Sales Team

お世話になっております。

先日ご提案に関して、以下の貴社製品の日本市場における販売拡大可能性を調査いたしました。

https://www.kickstarter.com/projects/...

フェーズ1：クラウドファンディング
フェーズ2：アマゾン等のECサイト販売
フェーズ3：日本国内主要量販店へ卸販売

【詳細な市場分析】

①日本における、クラファン及びECサイトにおける販売実績の有無
...
```

---

## 実施した修正内容

### 1. 日本語レポート生成プロンプトの修正

**ファイル**: `openai_client_improved.py`
**関数**: `_create_improved_japanese_prompt()`

**修正箇所1**: プロンプトの冒頭に書式指示を追加（83-88行目）
```python
【書式に関する非常に重要な指示】
※このレポートはメール本文として直接使用されます
※各段落の後には必ず空行（改行2回）を入れてください
※各セクション（①、②、③等）の後には必ず空行を2行入れてください
※箇条書きの各グループの後にも空行を入れてください
※メールマージで使用するため、改行が非常に重要です
※見やすさを最優先してください
※Markdown形式は使用せず、プレーンテキスト形式で出力してください
```

**修正箇所2**: 【重要な指示】セクションに改行指示を追加（257-258行目）
```python
9. 各段落の後には必ず空行（改行を2回）を入れて見やすくしてください
10. 各セクション（①、②、③等）の間には必ず空行を2行入れてください
```

### 2. 英語レポート生成プロンプトの修正

**ファイル**: `openai_client_improved.py`
**関数**: `_create_english_prompt()`

**修正箇所**: 【Important Instructions】セクションに改行指示を追加（393-394行目）
```python
5. Add a blank line (double line break) after each paragraph for readability
6. Add 2 blank lines between each section (①, ②, ③, ④)
```

**修正箇所**: 【Formatting Instructions】セクションに強調を追加（402行目）
```python
※LINE BREAKS ARE CRITICAL - Please ensure blank lines between all paragraphs and sections
```

---

## テスト手順

### 1. システムのテスト実行

```bash
# 新しいKickstarter URLで1件テスト
cd /Users/r.umeyama/work/kickstarter-market-analyzer
python3 main.py
```

### 2. Google Sheetsでの確認

1. Google Sheetsを開く
2. 生成された`jp_body`列のセルを確認
3. セル内で改行が適切に挿入されているか確認

**確認ポイント**:
- 挨拶文と本文の間に空行があるか
- 各フェーズの間に空行があるか
- 【詳細な市場分析】の前後に空行があるか
- 各セクション（①、②、③等）の間に空行が2行あるか
- 署名部分の前に空行があるか

### 3. CSV エクスポートでの確認

1. Google Sheetsで「ファイル」→「ダウンロード」→「カンマ区切り形式(.csv)」
2. ダウンロードしたCSVファイルをテキストエディタで開く
3. `jp_body`列の内容を確認

**確認方法**:
```bash
# CSVファイル内の改行を確認
cat kickstarter.csv | grep -A 50 "jp_body"
```

### 4. メールマージでの確認

1. CSVファイルをメールマージツールに読み込む
2. `jp_body`フィールドをメール本文として設定
3. プレビューで改行が正しく表示されるか確認

---

## 技術的な背景

### CSV内の改行の扱い

Google Sheetsセル内の改行は、CSV形式でエクスポートする際に以下のように処理される：

1. **セル内改行の保持**: セル内の改行文字（`\n`）は保持される
2. **ダブルクォートでの囲み**: 改行を含むフィールドは自動的にダブルクォート（`"`）で囲まれる
3. **改行文字**: LF（`\n`）またはCRLF（`\r\n`）として保存される

**正しいCSV形式の例**:
```csv
NO,product_url,jp_body
1,https://example.com,"メーカー名 Sales Team

お世話になっております。

先日ご提案に関して..."
```

### OpenAI API の出力における改行

OpenAI APIは、プロンプト内の指示に従って改行を挿入する。重要なポイント：

1. **明示的な指示が必要**: 「改行を入れてください」という明示的な指示が効果的
2. **テンプレートの空行**: プロンプト内のテンプレート部分に空行を含めると、出力にも反映されやすい
3. **temperature設定**: 0.7（現在の設定）は適度な創造性を持ちつつ、指示に従う

---

## トラブルシューティング

### ケース1: 改行が全く反映されない

**原因**: OpenAI APIが指示を無視している
**対処法**:
1. プロンプト内の改行指示をさらに強調
2. temperature を 0.5 に下げる（より指示に従いやすくなる）
3. system messageで改行の重要性を強調

### ケース2: 一部の改行のみ反映される

**原因**: OpenAI APIが一部の指示のみ適用
**対処法**:
1. 各セクションごとに改行指示を繰り返す
2. テンプレート内に明示的に空行を挿入
3. 出力後に後処理でpythonコードで改行を追加

### ケース3: CSV読み込み時に改行が消える

**原因**: メールマージツールの設定
**対処法**:
1. CSVファイルのエンコーディングを確認（UTF-8推奨）
2. メールマージツールの設定で「改行を保持」オプションを有効化
3. 代替案: Gmailの差し込み印刷機能を使用（改行対応が良い）

---

## 次のステップ

1. **実装のテスト実行**: 上記「テスト手順」に従って実行
2. **結果の確認**: CSV内の改行が正しく保持されているか確認
3. **メールマージテスト**: 実際のメールマージで本文が正しく表示されるか確認
4. **追加修正が必要な場合**: 上記トラブルシューティングを参照

---

## 補足: 代替案（必要に応じて実装）

修正が十分でない場合、以下の追加対策を検討：

### 1. 後処理での改行追加

```python
# main.pyまたはgoogle_sheets_client.pyで実装
def add_line_breaks(text):
    """レポート本文に明示的に改行を追加"""
    # セクション番号の後に改行を追加
    text = re.sub(r'(①|②|③|④|⑤|⑥)', r'\n\n\1', text)
    # URLの後に改行を追加
    text = re.sub(r'(https://[^\s]+)', r'\1\n\n', text)
    # フェーズの後に改行を追加
    text = re.sub(r'(フェーズ[123]：[^\n]+)', r'\1\n', text)
    return text
```

### 2. HTML形式でのメール送信

メールマージでHTML形式を使用できる場合：
```python
# 改行を<br>タグに変換
html_body = text.replace('\n', '<br>')
```

---

**作成者**: Claude Code
**最終更新**: 2025-11-14
